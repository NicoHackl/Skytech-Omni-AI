# Basisimages je Architektur, fest auf eine Alpine-Fassung genagelt. Ohne diese
# Datei zieht der Supervisor die jeweils neueste — ein Build von heute ergäbe
# dann ein anderes Bild als der von morgen.
build_from:
  aarch64: "ghcr.io/home-assistant/aarch64-base:3.19"
  amd64: "ghcr.io/home-assistant/amd64-base:3.19"
